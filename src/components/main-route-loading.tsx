import { MainPageLoadingPlaceholder } from "@/components/page-loading-placeholder";

/** Default `(main)` segment loading UI (skeleton cards). */
export default function MainRouteLoading() {
  return <MainPageLoadingPlaceholder />;
}
